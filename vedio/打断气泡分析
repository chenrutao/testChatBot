问题背景

 用户反馈：在TTS播报和LLM推理时，系统不聆听打断。

 一、状态机状态定义

 系统定义了5个对话状态（DialogState）：

 ┌────────────┬──────────────┬─────────────────────────────┐
 │    状态    │     含义     │          状态转换           │
 ├────────────┼──────────────┼─────────────────────────────┤
 │ IDLE       │ 空闲         │ → LISTENING                 │
 ├────────────┼──────────────┼─────────────────────────────┤
 │ LISTENING  │ 监听用户语音 │ → PROCESSING, IDLE          │
 ├────────────┼──────────────┼─────────────────────────────┤
 │ PROCESSING │ 处理ASR结果  │ → THINKING, LISTENING, IDLE │
 ├────────────┼──────────────┼─────────────────────────────┤
 │ THINKING   │ LLM推理中    │ → SPEAKING, LISTENING, IDLE │
 ├────────────┼──────────────┼─────────────────────────────┤
 │ SPEAKING   │ TTS播报中    │ → LISTENING, IDLE           │
 └────────────┴──────────────┴─────────────────────────────┘

 关键代码 (state_machine.py:15-21):
 TRANSITIONS = {
     DialogState.IDLE: [DialogState.LISTENING],
     DialogState.LISTENING: [DialogState.PROCESSING, DialogState.IDLE],
     DialogState.PROCESSING: [DialogState.THINKING, DialogState.LISTENING, DialogState.IDLE],
     DialogState.THINKING: [DialogState.SPEAKING, DialogState.LISTENING, DialogState.IDLE],
     DialogState.SPEAKING: [DialogState.LISTENING, DialogState.IDLE],
 }

 二、当前打断检测逻辑

 2.1 打断检测入口

 位置: system.py:191-195

 # ========== 2. SPEAKING状态下检测打断 ==========
 if self.dialog_state.state == DialogState.SPEAKING:
     if self.acoustic_vad.check_interrupt(audio_chunk):
         # 检测到人声，启动打断确认模式
         return await self._start_interrupt_confirmation()

 2.2 can_interrupt() 方法定义

 位置: state_machine.py:91-93

 def can_interrupt(self) -> bool:
     """是否可以打断"""
     return self._state == DialogState.SPEAKING

 2.3 is_busy() 方法定义

 位置: state_machine.py:95-97

 def is_busy(self) -> bool:
     """是否忙碌"""
     return self._state in [DialogState.PROCESSING, DialogState.THINKING, DialogState.SPEAKING]

 三、打断时序分析

 3.1 允许打断的状态

 ┌────────────┬──────────────┬──────────────────────────┐
 │    状态    │ 是否允许打断 │           说明           │
 ├────────────┼──────────────┼──────────────────────────┤
 │ SPEAKING   │ ✅ 允许      │ 唯一允许打断的状态       │
 ├────────────┼──────────────┼──────────────────────────┤
 │ LISTENING  │ ❌ 不需要    │ 正在监听用户输入         │
 ├────────────┼──────────────┼──────────────────────────┤
 │ PROCESSING │ ❌ 不允许    │ ASR结果处理阶段          │
 ├────────────┼──────────────┼──────────────────────────┤
 │ THINKING   │ ❌ 不允许    │ LLM推理阶段 - 不响应打断 │
 ├────────────┼──────────────┼──────────────────────────┤
 │ IDLE       │ ❌ 不需要    │ 空闲状态                 │
 └────────────┴──────────────┴──────────────────────────┘

 3.2 时间线分析

 用户说话 → [LISTENING] → ASR完成 → [PROCESSING] → 语义判断 → [THINKING] → LLM推理 → [SPEAKING] → TTS播报
                         ↑                                    ↑                      ↑
                         不检测打断                           不检测打断              检测打断

 3.3 关键问题

 核心问题：打断检测代码 check_interrupt() 只在 SPEAKING 状态下执行。

 这意味着：
 - LLM推理阶段（THINKING状态）：用户说话无法打断
 - TTS播报阶段（SPEAKING状态）：用户说话可以打断

 四、打断确认机制

 当检测到打断时，系统进入"打断确认模式"：

 4.1 流程

 1. 检测到人声 → 启动打断确认模式 (_start_interrupt_confirmation)
 2. 语义VAD判断是否为有效人声
 3. 有效人声 → 停止TTS，继续接收用户输入
 4. 非有效人声（咳嗽、嗯等）→ 取消打断，继续播报
 5. 超时（1500ms）→ 根据内容决定

 4.2 声学VAD打断检测阈值

 位置: acoustic_vad.py - 需要连续3帧（60ms）检测到人声才确认打断

 def check_interrupt(self, audio_frame: bytes) -> bool:
     is_speech = self._detect_speech(audio_frame)
     if is_speech:
         self._interrupt_speech_frames += 1
         if self._interrupt_speech_frames >= self._interrupt_threshold_frames:  # 默认3帧
             return True
     else:
         self._interrupt_speech_frames = 0
     return False

 五、气泡（时间空隙）分析

 5.1 完整流程时间线

 阶段                  状态          打断检测    时间长度
 ────────────────────────────────────────────────────────
 用户开始说话          →            -          -
 VAD检测到语音         LISTENING    不检测      ~100-300ms
 ASR流式处理           LISTENING    不检测      用户说话时长
 静音检测              LISTENING    不检测      500-2000ms
 ASR结果处理           PROCESSING   不检测      ~50-200ms
 语义VAD判断           PROCESSING   不检测      ~50-100ms
 ────────────────────────────────────────────────────────
 LLM推理               THINKING     ❌不检测    1-10秒（不可预测）
 ────────────────────────────────────────────────────────
 TTS开始播报           SPEAKING     ✅检测      -
 TTS播报中             SPEAKING     ✅检测      播报时长
 ────────────────────────────────────────────────────────

 5.2 无法打断的时间空隙

 最大空隙 = THINKING阶段时间 + PROCESSING阶段时间

 典型情况：
 - PROCESSING阶段：~100-300ms
 - THINKING阶段（LLM推理）：1-10秒（取决于LLM响应速度）
 - 总空隙：可能达到10秒以上

 六、问题总结

 当前机制的限制

 1. LLM推理期间（THINKING状态）完全不检测打断
   - 用户在这期间说话会被完全忽略
   - 需要等LLM开始输出、TTS开始播放才能打断
 2. 打断检测时机过晚
   - 必须等到SPEAKING状态才开始检测
   - 用户在LLM思考期间说的话无法触发打断
 3. 用户体验影响
   - 如果LLM推理时间长，用户可能重复说话
   - 系统不会响应，直到TTS开始播放

 七、潜在改进方向（待讨论）

 方案A：扩展打断检测到THINKING状态

 - 在THINKING状态也检测用户人声
 - 检测到打断时取消LLM请求

 方案B：在所有忙碌状态检测打断

 - PROCESSING和THINKING状态都检测打断
 - 使用interrupt标志控制后续流程

 方案C：后台持续监听

 - 无论什么状态，都持续进行VAD检测
 - 维护音频缓冲区，打断时可以从缓冲区开始处理

 ---
 文件路径参考:
 - 状态机定义: src/voice_dialog/core/state_machine.py
 - 主系统逻辑: src/voice_dialog/system.py
 - 声学VAD: src/voice_dialog/modules/acoustic_vad.py



系统并发模型分析报告                                                                                                  
  一、问题确认：是单线程阻塞模式                                                                                                                                                    t
  结论：当前系统在模型推理期间无法监听前端音频输入

  二、流程分析

  2.1 WebSocket 主循环 (websocket_server.py:184-195)

  while True:
      data = await websocket.receive()  # ① 等待接收消息
      # ...
      await handle_audio(client_id, audio_chunk)  # ② 阻塞点！

  2.2 调用链

  WebSocket.receive()
      ↓
  handle_audio()
      ↓ await
  system.process_audio()
      ↓ await (语音结束时)
  _finalize_streaming()
      ↓ await
  _process_with_llm()
      ↓ await (阻塞整个链路)
  llm_planner.plan_stream()

  2.3 问题根源

  ┌────────────┬──────────┬───────────────────┐
  │    阶段    │   状态   │  能否接收新音频   │
  ├────────────┼──────────┼───────────────────┤
  │ LISTENING  │ 等待语音 │ ✅ 可以           │
  ├────────────┼──────────┼───────────────────┤
  │ PROCESSING │ 处理 ASR │ ✅ 可以（短时间） │
  ├────────────┼──────────┼───────────────────┤
  │ THINKING   │ LLM 推理 │ ❌ 阻塞           │
  ├────────────┼──────────┼───────────────────┤
  │ SPEAKING   │ TTS 播报 │ ✅ 可以           │
  └────────────┴──────────┴───────────────────┘

  三、详细分析

  3.1 为什么 THINKING 状态会阻塞？

  # system.py:612
  return await self._process_with_llm(...)  # 阻塞等待 LLM 完成

  虽然使用了 async/await，但整个 _process_with_llm 是一个 顺序执行的 async 函数：

  # system.py:695-698
  llm_response = await self.llm_planner.plan_stream(...)  # 阻塞等待流式响应

  3.2 事件循环的工作方式

  时间线:
  ─────────────────────────────────────────────────────────
  T0: WebSocket 收到音频 → process_audio() 开始
  T1: 语音结束 → _process_with_llm() 开始
  T2: LLM 开始推理 → 事件循环等待
      │
      │  在此期间:
      │  - WebSocket 缓冲区积累新音频（但主循环被阻塞）
      │  - 无法调用 process_audio() 处理新音频
      │  - 打断检测代码无法执行
      │
  T3: LLM 推理完成 → _process_with_llm() 返回
  T4: process_audio() 返回 → WebSocket 主循环恢复
  T5: 处理积压的音频（但已经太晚了）

  3.3 代码证据

  打断检测代码确实存在 (system.py:191-196)：
  if self.dialog_state.state in [DialogState.SPEAKING, DialogState.THINKING]:
      if self.acoustic_vad.check_interrupt(audio_chunk):
          return await self._start_interrupt_confirmation()

  但问题是：这段代码在 process_audio() 内部，而 process_audio() 在 LLM 推理期间根本没有被调用。

  四、对比分析

  4.1 当前模式（阻塞）

  前端发送音频 ──────────────────────────────────────────►
                      │
                      ▼
                WebSocket 接收
                      │
                      ▼ await
                handle_audio()
                      │
                      ▼ await (阻塞)
                process_audio()
                      │
                      ▼ await (阻塞 ~2-10秒)
                _process_with_llm()
                      │
                      ▼
                LLM 推理完成
                      │
                      ▼
                返回，接收下一个音频

  4.2 理想模式（非阻塞）

  前端发送音频 ──────────────────────────────────────────►
                      │
                      ▼
                WebSocket 接收 (持续运行)
                      │
                      ├──────────────────┐
                      ▼                  ▼
                音频接收任务        LLM 推理任务 (后台)
                (持续监听)          (独立运行)
                      │                  │
                      ▼                  ▼
                检测打断 ────────► 取消 LLM 任务

  五、影响范围

  ┌──────────────────────┬───────────────────────────────┐
  │         场景         │             影响              │
  ├──────────────────────┼───────────────────────────────┤
  │ LLM 推理期间用户说话 │ 无法检测，必须等 LLM 完成     │
  ├──────────────────────┼───────────────────────────────┤
  │ 用户想打断 LLM 思考  │ 需要等 LLM 开始输出后才能打断 │
  ├──────────────────────┼───────────────────────────────┤
  │ 推理时间 > 3秒       │ 用户体验明显下降              │
  └──────────────────────┴───────────────────────────────┘

  六、解决方案建议

  方案 A：将 LLM 处理改为后台任务

  async def _finalize_streaming(self):
      # 不直接 await，而是创建后台任务
      self._llm_task = asyncio.create_task(self._process_with_llm(...))
      return None  # 立即返回，让 WebSocket 主循环继续

  方案 B：使用独立的音频接收循环

  async def start_audio_loop(self):
      while True:
          audio = await self.audio_queue.get()
          await self._process_audio_internal(audio)

  方案 C：在 LLM 流式输出中定期 yield

  async for chunk in stream:
      # 处理 chunk
      await asyncio.sleep(0)  # 让出控制权，允许处理其他任务

  七、结论

  当前系统存在并发缺陷：在 THINKING 状态下，系统确实是"单线程阻塞"的，无法实时监听前端音频输入。

  需要重构为：
  1. 音频接收与处理解耦
  2. LLM 推理作为后台任务运行
  3. 主循环持续监听音频，支持随时打断
